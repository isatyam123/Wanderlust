<% layout("/layouts/boilerPlate") %>
    <style>
        .filters{
            display: flex;
            flex-wrap: wrap;
            align-items: center;
        }

        .filter{
            text-align: center;
            margin-right: 1.5rem;
            margin-top: 2rem;
            opacity: 0.6;
        }

        .filter:hover{
            opacity: 1;
            cursor: pointer;
        }
        
        .filter p{
            font-size: 0.8rem;
        }

        #tax-toggle {
            display: flex;
            align-items: center;
            border: 1px solid gainsboro;
            border-radius: 1rem;
            padding: 1rem;
            height: 3rem;
            margin-top: 1rem;
        }

        .switch label{
            font-size: 0.9rem;
        }

        .tax-info {
            display: none;
        }

        @media (max-width: 768px) {
         .filters {
            display: flex;            /* already there */
            flex-wrap: nowrap;        /* one row only */
            overflow-x: auto;         /* enable horizontal scroll if children overflow */
            overflow-y: hidden;       /* no vertical scrollbars */
            max-width: 100%;          /* don't expand beyond the parent */
            white-space: nowrap;      /* helps if you use inline-blocks somewhere */
            -webkit-overflow-scrolling: touch; /* smooth scrolling on iOS */
         }

         .filter {
            flex: 0 0 auto;           /* each item keeps its size, won't shrink */
         }
         }

         .filters::-webkit-scrollbar {
            height: 6px;        /* thin horizontal scrollbar */
         }

         .filters::-webkit-scrollbar-thumb {
            background-color: rgba(0,0,0,0.2); /* visible thumb */
            border-radius: 3px;
         }

        /* Default style for larger screens */
         .card-img-top {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-radius: 12px;
         }

         /* Small screens (phones) */
         @media (max-width: 576px) {
         .card-img-top {
            aspect-ratio: 1 / 1;   /* keep square */
            width: 80%;            /* only take 80% of screen */
            max-width: 300px;      /* don’t let it grow too large */
            margin: 0 auto;        /* center it */
         }
         }



    </style>

    <div class="filters mt-3">
        <div class="filter">
           <div><i class="fa-solid fa-fire"></i></div> 
           <p>Trending</p>
        </div>
        <div class="filter">
            <div><i class="fa-solid fa-bed"></i></div> 
            <p>Rooms</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-water-ladder"></i></div> 
            <p>Amazing pools</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-mountain-city"></i>"></div> 
            <p>Iconic cities</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-key"></i></div> 
            <p>New</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-house"></i></div> 
            <p>Tiny homes</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-mountain"></i></div> 
            <p>Mountains</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-tower-observation"></i></div> 
            <p>Tower</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-umbrella-beach"></i></div> 
            <p>Beach</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-campground"></i></div> 
            <p>Camping</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-golf-ball-tee"></i></div> 
            <p>Golfing</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-sailboat"></i></div> 
            <p>Boats</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-tree"></i></div> 
            <p>Tropical</p>
         </div>
         <div class="filter">
            <div><i class="fa-solid fa-tractor"></i></div> 
            <p>Farms</p>
         </div>
         <div id="tax-toggle">
            <div class="form-check-reverse form-switch switch">
               <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault">
               <label class="form-check-label" for="flexSwitchCheckDefault">Display total before taxes</label>
             </div>
         </div>
    </div>
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-4">
        <% for(let listing of allListings){ %>
            <a href="/listings/<%= listing._id %>">
                <div class="card listing-card">
                    <img src="<%= listing.image.url %>" class="card-img-top" alt="/listing/image">
                    <div class="card-img-overlay"></div>
                    <div class="card-body">
                      <p class="card-text" style="text-align: center;">
                         <b><%= listing.title %></b><br>
                         &#8377;<%= listing.price.toLocaleString("en-IN") %>/night &nbsp;&nbsp;&nbsp;&nbsp;
                         <i class="tax-info">+18% GST</i>
                      </p>
                    </div>
                </div>
            </a>
        <% } %>
    </div>

    <script>
        let toggleSwitch = document.getElementById("flexSwitchCheckDefault");
           toggleSwitch.addEventListener("click", () => {
           let taxInfo = document.getElementsByClassName("tax-info");
           for(info of taxInfo){
            if(info.style.display !== "inline"){
               info.style.display = "inline";
            }
            else {
               info.style.display = "none";
            }
           }
        });
    </script>